// Must be customized, because you cannot use the name "Swift" as a module.

#include "Configurations/Extension.xcconfig"

INFOPLIST_FILE = Extensions/Swift/Info.plist
INFOPLIST_KEY_CFBundleDisplayName = Swift
PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_ID_PREFIX).Edit.Swift
PRODUCT_NAME = SwiftExtension
